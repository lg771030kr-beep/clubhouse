-- ================================================================
-- Club DX — Migration V5
-- 실행 방법: Supabase Dashboard > SQL Editor에서 실행
-- 작성일: 2026-04-16
-- ================================================================

-- clubs 테이블에 소개서 PDF URL 컬럼 추가
ALTER TABLE clubs
  ADD COLUMN IF NOT EXISTS brochure_url TEXT;   -- 소개서 PDF 공개 URL

-- ================================================================
-- 완료 체크리스트:
--   ✅ clubs.brochure_url — 소개서 PDF 저장 경로
-- ================================================================
