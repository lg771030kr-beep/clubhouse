-- ================================================================
-- Club DX — Migration V4
-- 실행 방법: Supabase Dashboard > SQL Editor에서 실행
-- 작성일: 2026-04-16
-- ================================================================

-- clubs 테이블에 소속 / 교내·연합 컬럼 추가
ALTER TABLE clubs
  ADD COLUMN IF NOT EXISTS affiliation TEXT,        -- 소속 학교·기관 (선택)
  ADD COLUMN IF NOT EXISTS club_type   TEXT;         -- '교내' | '연합' (선택)

-- ================================================================
-- 완료 체크리스트:
--   ✅ clubs.affiliation  — 소속 텍스트 필드
--   ✅ clubs.club_type    — 교내/연합 구분 필드
-- ================================================================
